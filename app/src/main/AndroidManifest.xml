<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.erikaosgue.introtokotlinfirebase">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntroToKotlinFirebase">
        <activity android:name="com.erikaosgue.introtokotlinfirebase.SignInAndSignOut.HomeActivity"></activity>
        <!-- <activity android:name=".MainActivity"> -->
        <!-- Comment the next line and uncomment the 2 lines above to launch main activity -->
        <activity android:name="com.erikaosgue.introtokotlinfirebase.SignInAndSignOut.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>